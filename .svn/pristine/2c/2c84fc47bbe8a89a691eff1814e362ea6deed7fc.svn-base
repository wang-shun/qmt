package com.lesports.qmt.msg.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import java.util.Set;

/**
 * User: ellios
 * Time: 15-11-10 : 下午2:55
 */
@Service("competitionSeasonService")
public class CompetitionSeasonService {
    private static final Logger LOG = LoggerFactory.getLogger(CompetitionSeasonService.class);

    public boolean changeLivePlatformsOfEpisodesRelatedWithCSeason(long csid, Set livePlatforms) {
        /*boolean result = SopsInternalApis.updateLivePlatformsOfEpisodesRelatedWithCSeason(csid, livePlatforms);
        if (!result) {
            LOG.info("fail to changeLivePlatformsOfEpisodesRelatedWithCSeason. csid : {}, livePlatforms : {}",
                    csid, livePlatforms);
        } else {
            LOG.info("success to changeLivePlatformsOfEpisodesRelatedWithCSeason. csid : {}, livePlatforms : {}",
                    csid, livePlatforms);
        }
        return result;*/
        return false;
    }
}
